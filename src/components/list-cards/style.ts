import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    marginBottom: 20,
    width: "90%",
    marginHorizontal: "auto",
    gap: 10,
  },
  subtitle: {
    fontSize: 22,
    textAlign: "center",
    marginBottom: 20,
    color: "white",
  },
});
